name: minimal
version: 1.0.0
description: Minimal single-phase workflow for simple projects

phases:
  - name: default
    order: 0
    template: default

project:
  name: default
  docs_dir: docs

validation:
  reference: base
